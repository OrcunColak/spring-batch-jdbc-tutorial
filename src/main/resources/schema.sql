CREATE TABLE person (
    id INT PRIMARY KEY,
    name VARCHAR(255),
    age INT
);

CREATE TABLE person_processed1 (
    id INT PRIMARY KEY,
    name VARCHAR(255),
    age INT
);

CREATE TABLE person_processed2 (
    id INT PRIMARY KEY,
    name VARCHAR(255),
    age INT
);
